--- src/main/services/RecentProjectsStore.ts
+++ src/main/services/RecentProjectsStore.ts
@@ -51,2 +51,10 @@
   async rename(id: string, newName: string): Promise<void> {
     const proj = this.cache.find((p) => p.id === id)
     if (proj) {
       proj.name = newName
       await this.save()
     }
   }
+
+  async updateProject(project: WorkspaceInfo): Promise<void> {
+    const index = this.cache.findIndex((p) => p.id === project.id)
+    if (index !== -1) {
+      this.cache[index] = { ...this.cache[index], ...project }
+      await this.save()
+    }
+  }
