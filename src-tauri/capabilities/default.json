{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capabilities for the CodeZ main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
