# Grok Build 请求上下文样例

当前只有源码，没有与 revision `8adf9013a...` 对齐的本机真实 runtime transcript。`01-source-derived-request.md` 使用源码中的主模板、Windows user prefix、ToolKind 映射和用户查询构造一份完整逻辑请求。

该样例标记为 `simulated/source-derived`，用于理解装配协议，不能当作 xAI 服务抓包。

九层源码档案与首条 user prefix 全文见 [context-layers](../context-layers/README.md)。
